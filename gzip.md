# Gzip

## ターミナルから操作する

### 圧縮する

`-k` オプションがない場合は元ファイルを削除する。

```sh
gzip [-k] FILES...

gzip [-k] -r DIRS...
```

### 伸長する

`-k` オプションがない場合は元ファイルを削除する。

```sh
gunzip [-k] FILES...

gunzip [-k] -r DIRS...
```
